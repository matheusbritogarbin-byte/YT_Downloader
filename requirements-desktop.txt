# Dependências para o executável desktop (PyWebView + yt-dlp)
# Instalar: pip install -r requirements-desktop.txt
# Compilar: pyinstaller --onefile --windowed --name "YT-Downloader" main_desktop.py

pywebview>=5.0.0
yt-dlp>=2025.1.15
httpx>=0.27.0